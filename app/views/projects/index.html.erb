<div class="page-header">
  <h1>
    Projects
    <a href="/projects/new" class="btn btn-lg btn-success">New Project</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>User</th>
        <th>Project Name</th>
        <th>Indirect costs</th>
        <th>Direct costs</th>
        <th>Gap dollar</th>
        <th>Gap percent</th>
        <th>Should cost</th>
        <th>Actual cost</th>
        <th>Industry</th>
        <th>Actions</th>
      </tr>

      <% @projects.each do |project| %>
      <tr>
        <td><%= project.user_id %></td>
        <td><%= project.name %></td>
        <td><%= project.indirect_costs %></td>
        <td><%= project.direct_costs %></td>
        <td><%= project.gap_dollar %></td>
        <td><%= project.gap_percent %></td>
        <td><%= project.should_cost %></td>
        <td><%= project.actual_cost %></td>
        <td><%= project.industry %></td>
        <td>
          <% if current_user.id==project.user_id %>
          <a href="/projects/<%= project.id %>" class="btn btn-primary">Show</a>
          <a href="/projects/<%= project.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_project/<%= project.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
          <% else %>
          <a href="/projects/<%= project.id %>" class="btn btn-primary">Show</a>
          <% end %>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
